export interface StarredTrack {
  userId: string;
  trackId: string;
  starredAt: string;
}

export interface StarredAlbum {
  userId: string;
  albumId: string;
  starredAt: string;
}

export interface StarredArtist {
  userId: string;
  artistId: string;
  starredAt: string;
}
