services:
  spherix:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: spherix
    restart: unless-stopped
    environment:
      - SESSION_SECRET=${SESSION_SECRET:-change-me-in-production}
    ports:
      - "80:80"
      - "3000:3000"
    volumes:
      - spherix_data:/data
      - /pfad/zu/deiner/musik:/music

volumes:
  spherix_data:
