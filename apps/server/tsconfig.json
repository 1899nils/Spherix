{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src",
    "customConditions": ["node"],
    "types": ["node", "bcryptjs", "cors", "express", "express-session", "multer", "uuid"]
  },
  "include": ["src"],
  "references": [
    { "path": "../../packages/shared" }
  ]
}
